{
  "name": "autotest-ai-quality-hub-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Project-local CLI tools for AutoTest AI CI and Android automation.",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "allure:version": "allure --version",
    "appium:version": "appium --version",
    "appium:drivers": "appium driver list --installed"
  },
  "devDependencies": {
    "allure-commandline": "2.43.0",
    "appium": "3.7.0"
  }
}
