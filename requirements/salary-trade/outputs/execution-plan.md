# 工资代理快速结算 - 场景级执行计划

- 状态：BLOCKED
- 生成时间：2026-09-02T18:49:56+08:00
- 场景数：8
- 业务价值：以业务场景为维护单位，把 Newman、JMeter、pytest 和人工复核收在同一个场景下，避免报告按工具散落。

## 场景清单

### 创建后申请人取消

- 状态：READY
- 用例：8 条
- 工具任务：newman、jmeter、pytest
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。

### 创建后代理拒绝

- 状态：READY_WITH_WARNINGS
- 用例：8 条
- 工具任务：newman、jmeter、pytest
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。

### 接受并转账后申请人确认完成

- 状态：READY_WITH_WARNINGS
- 用例：24 条
- 工具任务：newman、jmeter、pytest
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。

### 转账后申请人投诉，投诉失败

- 状态：READY_WITH_WARNINGS
- 用例：8 条
- 工具任务：newman、jmeter、pytest
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。

### 转账后申请人投诉，投诉成功

- 状态：READY_WITH_WARNINGS
- 用例：8 条
- 工具任务：newman、jmeter、pytest
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。

### 创建后代理不处理直到订单过期

- 状态：BLOCKED
- 用例：8 条
- 工具任务：newman、jmeter、pytest、manual
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。
- manual：人工复核运营处理、后台定时任务或长等待结果。

### 代理接受后不转账，12小时过期取消

- 状态：BLOCKED
- 用例：8 条
- 工具任务：newman、jmeter、pytest、manual
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。
- manual：人工复核运营处理、后台定时任务或长等待结果。

### 代理已转账后申请人24小时不确认，订单自动完成

- 状态：BLOCKED
- 用例：8 条
- 工具任务：newman、jmeter、pytest、manual
- 人工复核：这个场景是否按需求进入了目标订单状态，并且资金、日志、凭证证据一致？

- newman：运行额度、代理列表、订单详情等轻量接口预检，先确认鉴权和基础参数可用。
- jmeter：执行业务主流程状态机，提取同一个 orderNo 并传递到后续请求。
- pytest：执行后复核 HTTP 结果、DB订单/日志/凭证和可选Redis证据。
- manual：人工复核运营处理、后台定时任务或长等待结果。
