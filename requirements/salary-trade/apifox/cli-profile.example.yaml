schema_version: '1.1'
enabled: false
name: Apifox发布前核心冒烟
# 把Apifox的CI/CD页面生成命令拆成数组；真实access token只放环境变量。
command:
  - apifox-cli
  - run
required_env:
  - APIFOX_ACCESS_TOKEN
timeout_seconds: 300
report_globs: []
